{
  "name": "spree-and-a-half",
  "version": "0.1.0",
  "description": "Swarm-controlled sword game with side-scrolling platformer mechanics",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "vite",
    "serve": "vite preview",
    "start": "npm run build && npm run serve",
    "build:itch": "npm run build && npm run package:itch",
    "package:itch": "mkdir -p build && cp index.html build/ && cp -r dist build/ && cp -r assets build/"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "vite": "^4.0.0"
  },
  "keywords": ["game", "swarm", "sword", "side-scroller", "typescript", "canvas", "boids"],
  "author": "",
  "license": "MIT"
}
